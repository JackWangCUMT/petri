macro active à l'état initial mais en fait non
Condition qui dépend du résultat de l'action ne parse pas correctement (!REUSSI)

taille debugeditor
génération invocation compilateur
état interne des Conditions pas thread-safe (Gestion des timeouts)

Parenthèses A && B && C

Variables atomiques accessibles directement dans l'éditeur de RdP

Commenter le code :3
Documentation

Compilation du code : formattage des erreurs de compilation

Actions/Transitions : ajustement des dimensions à la longueur du titre

Fonction recherche

Scénario d'exécution

std::packaged_task
